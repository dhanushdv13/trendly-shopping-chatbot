version: '3.8'

services:
  trendly-agent:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    environment:
      # Explicitly set API_URL for Streamlit to hit the exposed port locally
      - API_URL=http://127.0.0.1:8000/chat
